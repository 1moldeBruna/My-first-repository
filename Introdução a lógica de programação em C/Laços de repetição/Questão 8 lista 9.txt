#include<stdio.h>


/*Um hotel cobra R$ 50,00 de diária por hóspede e mais uma taxa de serviços. A taxa de serviços é de:
 R$ 7,50 por diária, caso o número de diárias seja menor que 15;
 R$ 6,50 por diária, caso o número de diárias seja igual a 15;
 R$ 5,00 por diária, caso o número de diárias seja maior que 15.
 Faça um algoritmo que apresente as seguintes opções ao recepcionista:
 1. encerrar a conta de um hóspede
 2. verificar número de contas encerradas
3. finalizar a execução
Caso a opção escolhida seja a primeira, leia o nome e o número de diárias do hóspede e escreva o
nome e total a ser pago. Caso a opção escolhida seja a segunda, informe o número de hóspedes
que deixaram o hotel (número de contas encerradas).*/

int main () {

	int diaria, contas_encerradas =0;
	float total;
	char opcao;
	
	do 
	{

	printf("Qual opção deseja executar? \n");
	printf("Opção A para encerrar a conta de um hópede. \n");
	printf("Opção B para verificar o número de contas encerradas. \n");
	printf("Opção C para finalizar a execução\n");
	scanf("%c", &opcao);

	if(opcao == 'A' || opcao == 'a'){
	printf("Qual foi a diária do hóspede? \n");
	scanf("%d", &diaria);
		if(diaria<15){
		total = 50 + 7.50;
		printf("O total a pagar é: %f", total);
		}
		else{
			if(diaria == 15){
			total = 50 + 6.50;
			printf("O total a pagar é: %f", total);
				}
        		else{
				if(diaria>15){
				total = 50 + 5;
				printf("O total a pagar é: %f", total);		
              						}
            					}
          			
					}
				
				contas_encerradas = contas_encerradas + 1;				

				}
	 			 else{
					if(opcao == 'B' || opcao == 'b'){
					printf("O número de contas encerradas foi de: %d\n", contas_encerradas);
						}
	
					else{
						if(opcao == 'C' || opcao == 'c'){
						printf("Execução finalizada.\n");
						}
					}
         			}

 	 }while(opcao!= 'C' && opcao!= 'c');

	return 0;
}