#include<stdio.h>

/* Tentando descobrir se um dado era viciado, um dono de cassino honesto (BUrLAA!) o lançou n
vezes. Dados os n resultados dos lançamentos, determinar o número de ocorrências de cada face. */
int main () {

	int v[6],  Nlancamentos, lado, i;


	//*ao inicializar o código se é necessario zerar todos os valores dentro do vetor para que no fim ele não pegue coisas aleatórias da memória do pc*//
 	for(i=0; i<6; i++){
  	v[i] = 0; 
	}

	printf("Informe quantas vezes o dado foi lançado: ");
	scanf("%d", &Nlancamentos);

	for( i = 0; i<Nlancamentos; i++){

	printf("Insira o número da face que resultou do lançamento: \n");
	scanf("%d", &lado);
 	v[lado]++; //vai adicionar 1 ao número da face informado

	}

	for( i = 1; i<7; i++){

	//uso esse if para que na minha saida de dados apareça apenas os valores das faces que o usuário informou
	if(v[i]!=0){ 
	printf("O número de vezes que o dado caiu no número %d foi: %d\n", i, v[i]);


    }
  
	}


	return 0;
}