#include<stdio.h>
#include<math.h>

/*algoritimo que lê  matrícula do aluno, as notas das verificações, a média dos exercícios, a média de aproveitamento e informa se o mesmo foi aprovado ou não*/

int main () {

	int matricula; 
	int verificacaoParcial1, verificacaoParcial2, verificacaoParcial3, ME;
	float MA;

	printf("Matricula do aluno:");
	scanf("%d", &matricula);

	printf("Insira a nota parcial 1:");
	scanf("%d", &verificacaoParcial1);

	printf("Insira a nota parcial 2 :");
	scanf("%d", &verificacaoParcial2);

	printf("Insira a nota parcial 3:");
	scanf("%d", &verificacaoParcial3);

	printf("Insira a média dos exercícios: ");
	scanf("%d", &ME);

	//processamento 
	MA =  (verificacaoParcial1 + (verificacaoParcial2 * 2) + (verificacaoParcial3 * 3) + ME) / 7;


	if(MA>=9.0){
		printf("Parabéns %d, você recebeu o conceito A e está aprovado %d", matricula);
			}
		else{
			if(MA>=7.5 && MA <9.0){
				printf("Parabéns  %d, você recebeu o conceito B e está aprovado %d", matricula);
					}
			else{
				if(MA>=6.0 && MA<7.5){
					printf("Parabéns  %d, você recebeu o conceito C e está aprovado %d", matricula);
					}
				else{
					if(MA>=4.0 && MA<6.0){
						printf("Você recebeu o conceito D e infelizmente não foi aprovado %d", matricula);
								}
					else{
						if(MA<4.0){
							printf("Você recebeu o conceito E e infelizmente não foi aprovado %d", matricula);
								}
							}
						}
					}
				}
		return 0;
} 